# ml/fetch_harvests.py streams multi-gigabyte Metsäkeskus GeoPackages through here and keeps
# one disk-backed array per published part while it rasterizes. Nothing in it is an output.
ml/data/harvests/

__pycache__/
*.pyc

# fetch_fertilization.py caches the tukirekisteri stand centroids here while it measures
# them against the published raster. A working file, not an output: nothing in data/ is
# built from it.
ml/data/fertilization.json
